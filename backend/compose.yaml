services:
  db:
    image: docker.io/mysql:latest
    container_name: database
    environment:
      MYSQL_DATABSE: 'bestellservice'
      MYSQL_ROOT_PASSWORD: '###passwort###'
    ports:
      - "3306:3306"
    volumes:
      - /home/jakob/database:/var/lib/mysql
